---
title: 'DaisyUI-like Slider'
description: 'A simple slider styled like DaisyUI'
---
import { Code } from 'astro/components';
import { Icon } from 'astro-icon'
import RangeSlider from 'svelte-range-slider-pips';
import { cleanImport } from '~lib/cleanImport.ts';

import Demo from '~components/Reusable/Demo.svelte';
import Note from '~components/Reusable/Note.astro';

import Daisy from '~demos/recipes/daisyui/Daisy.svelte';
import DaisyRaw from '~demos/recipes/daisyui/Daisy.svelte?raw';
import DaisyCss from '~demos/recipes/daisyui/Daisy.css?raw';



Here I am trying to recreate the slider from [DaisyUI](https://daisyui.com/components/range/), but
adding my own bit of flare on with the `pips`, `labels` and colors

<Demo name="Demo of the Slider Styles" panel="css" client:visible>
  <Daisy client:visible />
</Demo>

<Note title="Breakdown" type="info">
  Most of the effort here is in the `CSS`, as you will find with most of the
  recipes. 
  
  There's a small amount of `JS` used to decide when the `float` `label`
  should flip to the other side of the handle. In this scenario it's around 20%.
</Note>

<Demo panel="css" client:visible>
  <Code slot="svelte" code={cleanImport(DaisyRaw)} lang="svelte" />
  <Code slot="css" code={DaisyCss} lang="css" />
</Demo>